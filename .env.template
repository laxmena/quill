TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_URL=
# Recommended: set to a long random string (openssl rand -hex 32) for HMAC verification
TELEGRAM_WEBHOOK_SECRET=
# Required for security: your numeric Telegram chat ID — find it via @userinfobot
# Without this, any user who discovers the bot can invoke commands and use your API quota
TELEGRAM_ALLOWED_CHAT_ID=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GMAIL_ADDRESS=
GMAIL_APP_PASSWORD=
BIOGRAPHY_RECIPIENT_EMAIL=
USER_NAME=Alex Rivera
BIOGRAPHY_PERIOD_DAYS=14
PROCESSING_HOUR=2
# Days of silence before Quill sends a gentle capture nudge (0 to disable)
NUDGE_AFTER_DAYS=3

# Set to true to run without real API keys — all external calls are simulated
QUILL_MOCK=false
